{
  "name": "ai-campus-companion",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "frontend": "npm --workspace frontend run dev",
    "backend": "npm --workspace backend run dev",
    "build": "npm --workspace frontend run build",
    "install-all": "npm install --workspaces"
  },
  "dependencies": {
    "mongoose": "^9.7.4"
  }
}
